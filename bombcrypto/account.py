class account:
  
  def __init__(self, name, backButton, mapButton, trunkButton, trunkCloseButton, heroButton, allPlayHeroButton, heroCloseButton):
      self.name = name
      self.backButton = backButton
      self.mapButton = mapButton
      self.trunkButton = trunkButton
      self.trunkCloseButton = trunkCloseButton
      self.heroButton = heroButton
      self.allPlayHeroButton = allPlayHeroButton
      self.heroCloseButton = heroCloseButton